services: 
  vaultwarden:
    image: vaultwarden/server:latest
    volumes: 
      - vaultwarden:/data
    ports: 
      - 8080:80
    restart: unless-stopped
    environment:
     - WEBSOCKET_ENABLED=true


volumes:
  vaultwarden:
    driver_opts:
      type: nfs
      o: "addr=truenas,nolock,soft,nfsvers=4uid=3000,gid=3001"
      device: ":/mnt/nas/kluster/vaultwarden"